import { HttpClient } from '@angular/common/http';
import { inject } from '@angular/core';

// Exemplo de URL da sua API (ajuste conforme seu Docker ou backend)
const API_URL = 'http://localhost:8080/api/auth'; // ou via proxy



